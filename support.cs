    
//  Math.abs(-100) -> 100 върща абсолютната стоиност на числото вътре
Math.Abs(totalFreeTime - durationEP);

// закръгля към по-голямото число след запетаята
Math.Ceiling(diff);

// закръгля към по-ниското число след запетаята
Math.Floor(salary);

// коренквадратен  
double numSqrt = Math.Sqrt(number);


       //--> let dayEven = (iDays % 2 == 0); //четен ден
       //--> let dayOdd = (iDays % 2 !== 0);  //нечетен ден
       //--> let evenHour = (jHour % 2 == 0) //четен час
       //--> let oddHour = (jHour % 2 == 1)  //нечетен час



// форматиране до 2-рия знак --->  200.22
Console.WriteLine($"You have {display:f2} leva left!");

// алтернативна конзола за показване на резултата
Console.WriteLine("{0:f2}%", p1);


// пример за кастване в конзол лога
Console.WriteLine($"{((double)wins / numberTurnirs) * 100:f2}%");



// деклариране на вход
int wwwww = int.Parse(Console.ReadLine());
double moneyInBankAccount = double.Parse(Console.ReadLine());
string moneyAction = Console.ReadLine();



// деклариране на променливи
int totalSteps = 0;
const int goalLimit = 10000;
double totalPrice = 0.0;
bool shouldContinue = true;
bool goalIsReached = false;
string lastProblem = string.Empty;



// работа с ковертиране на входни данни в различни променливи
string moneyInfo = Console.ReadLine(); //get is as 'string' to check for While condition

while(moneyInfo != "NoMoreMoney") {
    double dataIn = double.Parse(moneyInfo);
    .....
    moneyInfo = Console.ReadLine();
}



// фигура
else if (type == "circle") {   // лице на кръг
    double r = double.Parse(Console.ReadLine());
    //пи (3.14)  * r на квадрат
     double area = Math.PI * Math.Pow(r, 2);
     Console.WriteLine(area);
}